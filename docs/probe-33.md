# Probe for issue 33

A throwaway file. This branch exists to put a real pull request into the BEHIND
state so scripts/merge-pr.mjs can be shown refusing it. Both branch and file are
deleted afterwards.

