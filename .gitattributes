# Skills and process docs are read by agents on every platform. Normalise to LF
# in the repo so a Windows checkout does not publish CRLF into a plugin payload.
* text=auto eol=lf
*.png binary
*.jpg binary
